# 파비콘 추가 프롬프트

## 프롬프트 내용
`@index.html 문서에 @favicon.svg 를 파비콘으로 넣어`

## 설명
`index.html` 파일의 `<head>` 섹션에 `favicon.svg` 파일을 웹사이트의 파비콘으로 설정하는 `<link>` 태그를 추가하는 요청입니다. SVG 형식이므로 `type="image/svg+xml"` 속성을 포함합니다.

## 적용된 코드
```html
<link rel="icon" href="favicon.svg" type="image/svg+xml" />
```
